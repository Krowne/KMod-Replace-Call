<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools" package="com.kmod.replace.call">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name">
        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposedminversion" android:value="47+" />
        <meta-data android:name="xposeddescription" android:value="@string/app_desc" tools:ignore="ManifestResource" />
    </application>
</manifest>
